import 'package:flutter/material.dart';
import 'pages/profile_customization_screen.dart';

class ProfileEntry extends StatelessWidget {
  const ProfileEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ProfileCustomizationScreen();
  }
} 